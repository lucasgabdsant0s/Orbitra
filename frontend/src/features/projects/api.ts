import { api } from '@/lib/api';import type { Project } from '@/types';export const projectsApi = {  list: async (tenantId: string): Promise<Project[]> => {    const response = await api.get('/projects', { params: { tenantId } });    return response.data;  },  create: async (data: { name: string; description?: string; tenantId: string }): Promise<Project> => {    const response = await api.post('/projects', data);    return response.data;  },  update: async (id: string, data: { name?: string; description?: string }): Promise<Project> => {    const response = await api.put(`/projects/${id}`, data);    return response.data;  },  delete: async (id: string): Promise<void> => {    await api.delete(`/projects/${id}`);  },};